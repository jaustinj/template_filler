Repository to allow aggregation of VI exports

Steps:
1. Clone repository into new folder
2. Use "bash make.sh" in terminal to:
    -setup environment
    -download requirements
    -create an executable file to run script
3. Click executable (run.command) to run aggregator script (template_filler.py)

Feel free to rename/move executable as convenient
